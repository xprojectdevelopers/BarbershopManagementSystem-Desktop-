<Window x:Class="Capstone.LoginForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Capstone"
        mc:Ignorable="d"
        Title="Molave Street's Barbers - Login" 
        Height="375" 
        Width="500" 
        MinWidth="400" 
        MinHeight="400" WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibility"/>

        <!-- Define styles for responsive design -->
        <Style x:Key="TitleStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#FF1F4BFF"/>
            <Setter Property="Margin" Value="20,0"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#FF333333"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="InputStyle" TargetType="Control">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="35"/>
            <Setter Property="Background" Value="#E8E9EB"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0"/>
        </Style>

        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF1F4BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="25" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF0D3CE6"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF092BB8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ResponsiveProfileName" TargetType="TextBlock">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="0,15,0,5"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="ResponsiveProfileRole" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#757575"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="ResponsiveButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="MinWidth" Value="40"/>
            <Setter Property="MinHeight" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <ContentPresenter HorizontalAlignment="Center"
                          VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="TopBarIcon" TargetType="Image">
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
        </Style>
    </Window.Resources>

    <!-- Main Container with responsive layout -->
    <Border Background="White" CornerRadius="10" BorderBrush="white" BorderThickness="1" Height="400" VerticalAlignment="Center">
        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="446*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="10*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Height="60" VerticalAlignment="Top" Grid.ColumnSpan="2">
                <Grid Margin="0,-23,-40,43">

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,20,0">
                        <Button Click="BackButton_Click" Cursor="Hand" Style="{StaticResource ResponsiveButton}">
                            <Image Source="/x-square.png" Style="{StaticResource TopBarIcon}"/>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Header Section -->
            <StackPanel Grid.Row="0" Margin="4,20,-4,21" Grid.ColumnSpan="3">
                <Image Source="/Logo.png" 
                       Height="80"
                       HorizontalAlignment="Center"
                       Stretch="Uniform"/>

            </StackPanel>

            <!-- Login Form Section -->
            <StackPanel MaxWidth="350" HorizontalAlignment="Left" Margin="90,101,0,0" Grid.RowSpan="2">

                <!-- Employee ID Section -->
                <Label Content="Employee ID" Margin="-5,0,0,0" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="txtEmployeeId" VerticalContentAlignment="Center" 
                         Style="{StaticResource InputStyle}"
                         Margin="0,0,0,20"
                         FontSize="20"/>

                <!-- Password Section with Toggle -->
                <Label Content="Password" Margin="-5,0,0,0" Style="{StaticResource LabelStyle}"/>
                <Grid Width="278" Margin="0,0,0,30">
                    <PasswordBox x:Name="txtPassword" VerticalContentAlignment="Center"
                                 Height="35"
                                 FontSize="20"
                                 Background="#E8E9EB"
                                 BorderBrush="#E8E9EB"
                                 BorderThickness="1"
                                 Padding="10,0,40,0"/>

                    <TextBox x:Name="txtPasswordVisible" 
                             Height="35"
                             FontSize="20"
                             Background="#E8E9EB"
                             BorderBrush="#E8E9EB"
                             BorderThickness="1"
                             Padding="10,0,40,0"
                             Visibility="Collapsed"/>

                    <!-- Toggle Button -->
                    <Button x:Name="btnTogglePassword" 
                            Click="TogglePasswordVisibility_Click"
                            Width="30" Height="30"
                            HorizontalAlignment="Right"
                            Margin="0,0,5,0"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            ToolTip="Show/Hide Password">
                        <TextBlock x:Name="txtToggleIcon" Text="👁" FontSize="16" FontFamily="Segoe UI Emoji"/>
                    </Button>
                </Grid>

                <!-- Login Button -->
                <Button x:Name="btnLogin" 
                        Content="Log In" 
                        Style="{StaticResource LoginButtonStyle}"
                        Click="Button_Click_1" Cursor="Hand"
                        Background="black"
                        Margin="0,0,0,20" Width="132"/>

                <!-- Status Message -->
                <Label x:Name="lblStatus" 
                       Content="" 
                       FontSize="12" 
                       Foreground="#FFDC3545"
                       HorizontalContentAlignment="Center"
                       Margin="0,-20,0,0"/>
            </StackPanel>

        </Grid>
    </Border>
</Window>