<Window x:Class="Capstone.AppointmentOptions.DeclineAppointment"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Capstone.AppointmentOptions"
        mc:Ignorable="d"
        Title="DeclineAppointment" Height="605" Width="414" 
        ResizeMode="CanResizeWithGrip"
        WindowStartupLocation="CenterOwner" WindowStyle="None"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style x:Key="TextBoxStyleComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border Name="Border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="4">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Content Presenter -->
                                    <ContentPresenter Grid.Column="0"
                                              x:Name="PART_ContentPresenter"
                                              Margin="{TemplateBinding Padding}"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              HorizontalAlignment="Left"
                                              IsHitTestVisible="False"/>

                                    <ToggleButton Grid.Column="1"
                                          x:Name="PART_DropDownToggle"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Width="20"
                                          Height="20"
                                          Margin="0,0,8,0"
                                          VerticalAlignment="Center"
                                          IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                        <Path Fill="Gray"
                                      Data="M7,10L12,15L17,10H7Z"
                                      Stretch="Uniform"
                                      Width="10"
                                      Height="8"/>
                                    </ToggleButton>

                                    <!-- Popup -->
                                    <Popup x:Name="PART_Popup"
                                   Grid.ColumnSpan="2"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   PlacementTarget="{Binding ElementName=Border}"
                                   StaysOpen="False">
                                        <Border Background="White"
                                        BorderBrush="Gray"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        MinWidth="{Binding ActualWidth, ElementName=Border}">
                                            <ScrollViewer>
                                                <ItemsPresenter/>
                                            </ScrollViewer>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <!-- Hover effect -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#333"/>
                            </Trigger>
                            <!-- Focus effect -->
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#0078D4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Border BorderBrush="Black" BorderThickness="1" Padding="20" CornerRadius="8" Margin="20,20,20,20">
            <StackPanel>
                <!-- Image -->
                <Image Height="170" Width="200" 
                       Source="/AppointmentOptions/asking-question.png"
                       HorizontalAlignment="Center"
                       Margin="0,20,0,30"/>

                <!-- Reason for Decline Section -->
                <Label Content="Reason for Decline" 
                       FontWeight="Bold" 
                       FontSize="20" 
                       HorizontalAlignment="Left"
                       Margin="35,0,0,10"/>

                <ComboBox x:Name="cmbItemID" 
                          Height="47" 
                          FontSize="16" 
                          SelectedIndex="-1" 
                          Style="{StaticResource TextBoxStyleComboBox}"
                          Margin="0,0,0,5" Width="265">
                    <ComboBoxItem IsEnabled="False" IsSelected="True" Foreground="Gray">
                        Select reason for decline
                    </ComboBoxItem>
                    <ComboBoxItem>Time slot unavailable</ComboBoxItem>
                    <ComboBoxItem>Barber unvailable</ComboBoxItem>
                    <ComboBoxItem>Service unvailable</ComboBoxItem>
                    <ComboBoxItem>Short notice request</ComboBoxItem>
                    <ComboBoxItem>Shop closed/holiday</ComboBoxItem>
                </ComboBox>

                <TextBlock x:Name="cmbItemIDError" 
                           Foreground="Red" 
                           FontSize="12" 
                           Margin="0,5,0,20"
                           Visibility="Collapsed"/>


                <!-- Confirm Decline Button (RED) -->
                <Button Content="Confirm Decline"
                        Width="265" 
                        Cursor="Hand"
                        Height="50"
                        HorizontalAlignment="Center"
                        Foreground="White" 
                        FontSize="20" 
                        FontWeight="Bold" 
                        Margin="0,20,0,10">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="#E02C45" 
                                    CornerRadius="30" 
                                    BorderBrush="Transparent" 
                                    BorderThickness="0">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"
                                                  TextBlock.Foreground="White"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Back Button (BLACK) -->
                <Button Content="Back"
                        Width="265" 
                        Cursor="Hand"
                        Height="50"
                        HorizontalAlignment="Center"
                        Foreground="White" 
                        FontSize="20" 
                        FontWeight="Bold" 
                        Margin="0,0,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="#000000" 
                                    CornerRadius="30" 
                                    BorderBrush="Transparent" 
                                    BorderThickness="0">
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  RecognizesAccessKey="True"
                                                  TextBlock.Foreground="White"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>